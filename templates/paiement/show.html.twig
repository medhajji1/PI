{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idpaiement</th>
                <td>{{ paiement.idpaiement }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement.montant }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ paiement.date ? paiement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ paiement.motif }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ paiement.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paiement_index') }}">back to list</a>

    <a href="{{ path('app_paiement_edit', {'idpaiement': paiement.idpaiement}) }}">edit</a>

    {{ include('paiement/_delete_form.html.twig') }}
{% endblock %}
